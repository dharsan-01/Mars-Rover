// Interface defining the Observer role in the Observer pattern
public interface Observer {
    // Method to update the observer with a message when the subject changes
    void update(String message);
}
