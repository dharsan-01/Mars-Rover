package util;

public class Logger {
    // simple logging mechanism to log messages to the console
    public static void log(String message) {
        System.out.println("[LOG] " + message);
    }
}
