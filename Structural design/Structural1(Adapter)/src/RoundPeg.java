public class RoundPeg {
    private double radius;

    // Constructor to initialize the peg's radius
    public RoundPeg(double radius) {
        this.radius = radius;
    }

    // Getter method for the peg's radius
    public double getRadius() {
        return radius;
    }
}
